@model IEnumerable<MSMA_Proyecto.Models.Cocteles>

@{
    ViewData["Title"] = "Index";
}

<h1>Cocteles</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Coctel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripción)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Precio_Coctel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Coctel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripción)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Precio_Coctel)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDCocteles">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDCocteles">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDCocteles">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
